name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  control-tower:
    name: Control tower (lint + test)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: control-tower

    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.11"
          enable-cache: true
          cache-dependency-glob: "control-tower/pyproject.toml"

      - name: Install package + dev deps
        run: uv pip install --system -e ".[dev]"

      - name: Ruff
        run: ruff check .

      - name: Pytest (offline suite)
        # addopts already excludes @pytest.mark.live and @pytest.mark.postgres
        run: pytest -q

  sdk:
    name: AiGuard SDK
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: control-tower/sdk/aiguard

    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.11"
          enable-cache: true
          cache-dependency-glob: "control-tower/sdk/aiguard/pyproject.toml"

      - name: Install package + langchain extras
        run: uv pip install --system -e ".[dev,langchain]"

      - name: Pytest
        run: pytest -q
