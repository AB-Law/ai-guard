[project]
name = "control-tower"
version = "0.1.0"
description = "Aegis — control tower for enterprise agents"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "langgraph",
    "langchain-openai",
    "chromadb",
    "langchain",
    "fastapi",
    "uvicorn",
    "streamlit",
    "pydantic>=2.0",
    "pyyaml",
    "httpx",
    "python-dotenv",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio",
    "pytest-cov",
    "ruff",
]
# Only needed for the sqlite/postgres storage tiers (DATABASE_URL set) — the
# default in-memory/sqlite-audit-log tier needs none of this.
scale = [
    "langgraph-checkpoint-sqlite",
    "langgraph-checkpoint-postgres",
    "psycopg[binary]",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = [
    "agent*",
    "api*",
    "audit*",
    "configs*",
    "contracts*",
    "dashboard*",
    "guardrails*",
    "knowledge*",
    "investigation_assistant*",
    "scripts*",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
markers = [
    "live: tests that call the live OpenAI API",
    "postgres: tests that need AEGIS_TEST_POSTGRES_URL (a running Postgres)",
]
addopts = "-m 'not live and not postgres'"

[tool.ruff]
line-length = 100
target-version = "py311"
